import { FournisseurModel } from './fournisseur-model.model';

describe('FournisseurModel', () => {
  it('should create an instance', () => {
    expect(new FournisseurModel()).toBeTruthy();
  });
});
